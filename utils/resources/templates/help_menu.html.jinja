<!DOCTYPE html>
<html>
<head>
<style>
    :root {
        --primary-color: #2ac3ff;
        --secondary-color: #f1f8fe;
        --text-dark: #334155;
        --text-light: #64748b;
        --border-color: #e2e8f0;
        --shadow-color: rgba(42, 195, 255, 0.15);
        --card-radius: 16px;
    }
    
    body { 
        margin: 0; padding: 0; background: transparent; 
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; 
    }
    
    .help-container {
        width: 800px;
        min-height: 100vh;
        background-color: var(--secondary-color);
        background-image: url('{{ bg_image_uri }}');
        background-size: cover;
        background-position: center;
        border-radius: var(--card-radius);
        overflow: hidden; 
        color: var(--text-dark);
        position: relative;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    }
    
    .help-container::before { 
        content: ""; position: absolute; top: 0; left: 0; right: 0; bottom: 0; 
        background: rgba(255, 255, 255, 0.92); 
        backdrop-filter: blur(10px);
        z-index: 0; 
    }
    
    .banner, .content, .footer { position: relative; z-index: 1; }
    
    .banner { 
        background: linear-gradient(135deg, var(--primary-color) 0%, #1daaf0 100%);
        color: white; 
        padding: 35px 30px; 
        text-align: left;
        display: flex;
        justify-content: space-between;
        align-items: center;
        box-shadow: 0 4px 15px var(--shadow-color);
    }
    
    .banner-title {
        display: flex;
        flex-direction: column;
    }
    
    .banner h1 { 
        margin: 0; font-size: 34px; font-weight: 800; letter-spacing: 0.5px;
    }
    
    .banner-sub {
        margin-top: 6px; font-size: 15px; opacity: 0.9; font-weight: 500;
    }
    
    .content { 
        padding: 30px; 
        display: grid; 
        grid-template-columns: 1fr 1fr; 
        gap: 24px; 
    }
    
    .category { 
        background: white; 
        padding: 24px; 
        border-radius: var(--card-radius); 
        box-shadow: 0 4px 20px rgba(0,0,0,0.03); 
        border: 1px solid rgba(42, 195, 255, 0.1);
        transition: transform 0.2s;
    }
    
    .category-name { 
        display: inline-block;
        font-size: 17px; 
        font-weight: 700; 
        color: var(--primary-color); 
        margin-bottom: 20px; 
        padding: 6px 14px;
        background: var(--secondary-color);
        border-radius: 20px;
    }
    
    .cmd-item { 
        margin-bottom: 16px; 
        padding-bottom: 12px;
        border-bottom: 1px solid var(--border-color);
    }
    
    .cmd-item:last-child {
        margin-bottom: 0; padding-bottom: 0; border-bottom: none;
    }
    
    .cmd-name { 
        font-weight: 700; 
        color: var(--text-dark); 
        font-size: 15px;
        display: flex;
        align-items: center;
    }
    
    .cmd-name::before {
        content: "•";
        color: var(--primary-color);
        font-size: 20px;
        margin-right: 8px;
        line-height: 1;
    }
    
    .cmd-desc { 
        color: var(--text-light); 
        font-size: 13px; 
        margin-top: 6px;
        margin-left: 18px;
        line-height: 1.4;
    }
    
    .footer { 
        text-align: center; 
        padding: 20px; 
        color: var(--text-light); 
        font-size: 13px; 
        font-weight: 500;
        background: linear-gradient(to top, rgba(0,0,0,0.03), transparent);
        border-top: 1px solid var(--border-color);
    }
    
    .footer strong {
        color: var(--primary-color);
    }
</style>
</head>
<body>
<div class="help-container">
    <div class="banner">
        <div class="banner-title">
            <h1>bilibili_push</h1>
            <div class="banner-sub">B站动态与直播全场景推送引擎</div>
        </div>
        <div>
            <!-- Decorative icon could go here if needed -->
        </div>
    </div>
    <div class="content">
        {% for cat in categories %}
        <div class="category">
            <div class="category-name">{{ cat.name }}</div>
            {% for cmd in cat.commands %}
            <div class="cmd-item">
                <div class="cmd-name">{{ cmd.name }}</div>
                <div class="cmd-desc">{{ cmd.desc }}</div>
            </div>
            {% endfor %}
        </div>
        {% endfor %}
    </div>
    <div class="footer">AstrBot Bilibili Plugin • <strong>Premium Experience</strong></div>
</div>
</body>
</html>
